export interface AuthResponse {
	message: string;
}

export interface GenericResponse {
	message: string;
}

export interface LoginResponse {
	pid: string;
	email: string;
	name: string;
	createdAt: string;
}
